spring:
  datasource:
    url: jdbc:h2:~/hibernate;AUTO_SERVER=TRUE
    username: migrations
    password: hibernatePassword
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
